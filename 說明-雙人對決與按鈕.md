# 佛學知識王 — 雙人對決 & 藍牙搶答按鈕 說明

## 一、遊戲怎麼玩

雙擊 `啟動.bat`(Mac 為 `啟動.command`) → 瀏覽器打開 `http://localhost:3000` → 首頁有兩個模式:

- **單人挑戰**:原本的模式,10 題限時搶答、越快越高分、連擊加成。
- **雙人對決**:比分制。兩位玩家用各自的藍牙按鈕搶答同一題,
  答對即得分(**答得越快分數越高**,連續答對還有連擊加成);
  共 10 題,答完後**總分高者獲勝**。

後台題庫管理仍在 `http://localhost:3000/admin`。

---

## 二、按鈕硬體

### 材料
- ESP32-C3 SuperMini × 2(一人一組)
- 按鈕 × 4/組(A、B、C、D)

### 接線(每一組都一樣)
| 按鈕 | 接腳 | 另一腳 |
|------|------|--------|
| A | GPIO4 | GND |
| B | GPIO5 | GND |
| C | GPIO6 | GND |
| D | GPIO7 | GND |

程式用「內部上拉」,所以按鈕只要一腳接 GPIO、一腳接 GND 即可,**不用額外電阻**。
板載 LED(GPIO8)會在藍牙連上電腦後恆亮,方便確認連線。

---

## 三、燒錄韌體(PlatformIO)

專案在 `quiz_ranger_button/`。用 VS Code + PlatformIO 開啟該資料夾。

1. **第一塊板(玩家一)**:打開 `src/main.cpp`,確認最上面是
   ```cpp
   #define PLAYER 1
   ```
   接上第一塊板,按 PlatformIO 的 **Upload**(→ 燒錄)。

2. **第二塊板(玩家二)**:把那行改成
   ```cpp
   #define PLAYER 2
   ```
   接上第二塊板,再 Upload 一次。

> 第一次編譯會自動下載藍牙鍵盤函式庫(ESP32-BLE-Keyboard + NimBLE),需連網。

按鍵對應(網頁靠不同按鍵區分兩位玩家):
- 玩家一 A/B/C/D → `1 2 3 4`
- 玩家二 A/B/C/D → `7 8 9 0`

---

## 四、藍牙配對

1. 板子通電後,到 Windows **設定 → 藍牙與裝置 → 新增裝置 → 藍牙**。
2. 會看到 **button1** 與 **button2**,分別點選配對(免密碼)。
3. 配對成功後,兩塊板就像兩個藍牙鍵盤,按鍵會直接送到目前的視窗。
4. 回到遊戲「雙人對決 → 設定頁」,按各顆按鈕,畫面上對應的格子會亮起 = 正常。

> **重點:開始對決前,請先用滑鼠點一下遊戲網頁**,讓瀏覽器視窗保持在最前面(有焦點),
> 這樣按鈕送出的按鍵才會被網頁收到。

---

## 五、沒有硬體也能玩 / 測試

在對決設定頁或對決中,直接用**電腦鍵盤**即可:
- 玩家一:`1` `2` `3` `4`(= A B C D)
- 玩家二:`7` `8` `9` `0`(= A B C D)

---

## 六、常見問題

- **按了沒反應?** 先確認:(1) 板載 LED 有亮(藍牙已連);(2) 有先點一下網頁讓它取得焦點。
- **兩位玩家搶到同一格?** 不會混淆——兩塊板送的是不同按鍵,網頁依此分辨玩家一/二。
- **按鍵和電腦其他操作衝突?** 對決進行時盡量別切換視窗;搶答鍵是數字鍵,平常不影響。
- **想換腳位或按鍵?** 改 `src/main.cpp` 最上方的 `BTN_PIN[]` 或 `KEYS[]` 再重新燒錄。

---

## 七、技術備註

- ESP32-C3 只有 BLE(低功耗藍牙),沒有傳統藍牙 SPP,所以採用 **BLE HID 鍵盤**方式,
  最穩、免驅動、跨瀏覽器。
- 網頁端在 `index.html` 的 `Input` 模組監聽這些按鍵,`Duel` 模組負責對戰邏輯。
